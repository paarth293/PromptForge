# PromptForge Backend Configuration
# Fill in at least ONE LLM provider key below to get real (non-mock) chat responses.

PROMPTFORGE_ENV=development
PORT=8000
HOST=0.0.0.0
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# --- LLM Provider Keys ---
# Recommended: Groq is free and fast (get a key at https://console.groq.com/keys)
# The app auto-falls-back to Groq for any OpenAI-named model (e.g. gpt-4o) if
# OPENAI_API_KEY is empty and GROQ_API_KEY is set — so this one key alone is
# enough to get real chat responses instead of the mock/demo fallback.
GROQ_API_KEY=

# Optional — set any of these instead of, or in addition to, Groq:
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Local/offline model support (used by the Red Team attacker personas)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
OLLAMA_ENABLED=true

# --- Tool integrations (optional) ---
STRIPE_TEST_SECRET_KEY=

# --- Database & Tenancy ---
DATABASE_URL=sqlite+aiosqlite:///./promptforge.db
TENANT_DEFAULT_ID=tenant-demo
JWT_SECRET=dev-insecure-secret-promptforge-key
DEMO_MODE=true
